from setuptools import setup

setup(
   name='pyroofiller',
   version='0.1',
   description='A module to fill ROOT objects on a distributed computing cluster.',
   author='Lukas Adamek',
   author_email='lukas.adamek@mail.utoronto.ca',
   packages=['pyroofiller'],
   install_requires=[],
)
